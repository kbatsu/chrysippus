# OS
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.\#*
\#*\#

# Node (used by some tooling, not at runtime)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.pnpm-store/

# Python (used by some tooling, not at runtime)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# Eval outputs
evals/runs/
evals/results/
*.eval.json

# Generated artifacts (re-rendered by scripts/render.sh; should never be committed manually)
# NOTE: .claude/skills/, AGENTS.md, GEMINI.md, .cursor/rules/, .windsurfrules,
# CONVENTIONS.md, .clinerules ARE committed (downstream consumers depend on them
# being present in the repo). They are regenerated by render.sh and verified by
# check.sh in CI.

# Local secrets / drafts
.env
.env.*
!.env.example
*.local.md

# Build / dist
dist/
build/
*.tgz
*.tar.gz

# Logs
*.log
logs/
